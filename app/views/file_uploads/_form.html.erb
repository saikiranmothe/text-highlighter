<%= form_for(@file_upload, :html => {:multipart => true} ) do |f| %>
  <% if @file_upload.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@file_upload.errors.count, "error") %> prohibited this file_upload from being saved:</h2>

      <ul>
      <% @file_upload.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :select_a_file_for_uploading %>
    <%= f.file_field :text_file,placeholder: "Enter the file name" %>
  </div><br/>
  <div class="field">
    <%= f.label :name_of_the_text_file %>
    <%= f.text_field :file_name,placeholder: "Enter the file name"  %>
  </div><br/>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
